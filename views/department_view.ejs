<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/main.css">
    <title>Dash</title>
</head>

<body>

<div class="content">
    <ul class="breadcrumbs">
        <li> <a href="/"> Dash</a> </li>
        <li> / </li>
        <li> <a href="/departments">Departments</a> </li>
        <li> / </li>
        <li> View </li>
    </ul>
    <% if (department) { %>
        <h1><%= department.Dept_Name %></h1>
        <ul>
            <h2>Department Information</h2>
            <li> Department ID: <%= department.Dept_ID %> </li>
            <li> Department Location: <%= department.Dept_Location %> </li>
        </ul>
        <% if (courseInfo.length > 0) { %>
            <h2>Course Information</h2>
            <ul>
                <% courseInfo.forEach(course => { %>
                    <li> Course Code: <%= course.Crs_Code %> </li>
                    <li> Course Title: <%= course.Crs_Title %> </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>No course information found for this department.</p>
        <% } %>
    <% } else { %>
        <p>No department found</p>
    <% } %>
</div>

</body>

</html>